import { useEffect,useState } from 'react';import { Database,Save,ShieldCheck } from 'lucide-react';import { PageHeader } from '@/components/PageHeader';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';import { StorageSummary } from '@/components/StorageSummary';import { useAppData } from '@/hooks/useAppData';import type { AppSettings } from '@/models';
export function SettingsPage(){const {settings,saveSettings}=useAppData(),[draft,setDraft]=useState<AppSettings|null>(settings),[saved,setSaved]=useState(false);useEffect(()=>setDraft(settings),[settings]);if(!draft)return null;return <div className="space-y-6 p-4 md:p-6"><PageHeader title="Configuración" subtitle="Preferencias para el trabajo de campo"/><div className="rounded-lg border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-2 font-bold"><ShieldCheck className="size-5 text-primary"/>Calidad GPS</div><Label className="mt-5 block" htmlFor="accuracy">Precisión GPS máxima</Label><div className="mt-2 flex items-center gap-2"><Input id="accuracy" type="number" min={1} max={100} value={draft.maxGpsAccuracy} onChange={e=>setDraft({...draft,maxGpsAccuracy:Number(e.target.value)})}/><span className="text-sm font-semibold">m</span></div><p className="mt-2 text-xs text-muted-foreground">No se permitirá guardar cuando la precisión sea mayor.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><Readonly label="Unidad de distancia" value={draft.distanceUnit}/><Readonly label="Coordenadas" value={draft.coordinateDisplay}/><Readonly label="Formato" value={draft.coordinateFormat}/><Readonly label="Tema" value={draft.theme}/></div><Button className="mt-5 h-11 w-full" onClick={async()=>{await saveSettings(draft);setSaved(true);setTimeout(()=>setSaved(false),1800)}}><Save/>{saved?'Configuración guardada':'Guardar configuración'}</Button></div><StorageSummary/><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2 font-bold"><Database className="size-5 text-primary"/>Base local</div><p className="mt-2 text-sm text-muted-foreground">Válvulas, fotografías y levantamientos permanecen en este dispositivo. No se borran automáticamente.</p></div></div>};function Readonly({label,value}:{label:string;value:string}){return <div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 font-semibold capitalize">{value}</p></div>}
