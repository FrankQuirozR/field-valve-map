import { createFileRoute } from '@tanstack/react-router';import { SettingsPage } from '@/pages/SettingsPage';
export const Route=createFileRoute('/configuracion')({head:()=>({meta:[{title:'Configuración — App Válvulas'},{name:'description',content:'Configura precisión GPS y almacenamiento local de campo.'},{property:'og:title',content:'Configuración — App Válvulas'},{property:'og:description',content:'Configura precisión GPS y almacenamiento local de campo.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:SettingsPage});
