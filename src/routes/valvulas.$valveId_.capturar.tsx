import { createFileRoute } from '@tanstack/react-router';import { CapturePage } from '@/pages/CapturePage';
export const Route=createFileRoute('/valvulas/$valveId_/capturar')({head:()=>({meta:[{title:'Capturar ubicación — App Válvulas'},{name:'description',content:'Captura GPS local con validación de precisión.'},{property:'og:title',content:'Capturar ubicación — App Válvulas'},{property:'og:description',content:'Captura GPS local con validación de precisión.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Capture});function Capture(){return <CapturePage valveId={Route.useParams().valveId}/>}
