import { Link } from '@tanstack/react-router'; import { ChevronRight, Navigation } from 'lucide-react'; import type { Valve } from '@/models'; import { StatusBadge } from './StatusBadge';
export function ValveListItem({valve,distance}:{valve:Valve;distance?:number}){return <Link to="/valvulas/$valveId" params={{valveId:valve.id}} className="flex items-center gap-3 border-b border-border bg-card px-4 py-4 last:border-0 hover:bg-accent/50"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-info-soft text-info"><span className="size-4 rounded-full border-[5px] border-current"/></span><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><strong className="text-sm">{valve.codigo}</strong><StatusBadge status={valve.estado}/></div><p className="mt-1 truncate text-xs text-muted-foreground">{valve.tipo} · {valve.diametro} · {valve.sector}</p>{distance!=null&&<p className="mt-1 flex items-center gap-1 text-xs font-semibold text-primary"><Navigation className="size-3"/>{distance<1000?`${Math.round(distance)} m`:`${(distance/1000).toFixed(1)} km`}</p>}</div><ChevronRight className="size-5 text-muted-foreground"/></Link>}
