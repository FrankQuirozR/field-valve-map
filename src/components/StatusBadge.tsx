export function StatusBadge({status}:{status:string}){const good=status.toLowerCase().includes('operativa')&&!status.toLowerCase().includes('revisión');return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-bold ${good?'bg-success-soft text-success':'bg-warning-soft text-warning-foreground'}`}><span className={`size-2 rounded-full ${good?'bg-success':'bg-warning'}`}/>{status}</span>}
