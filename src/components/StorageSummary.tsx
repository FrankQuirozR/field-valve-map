import { useEffect,useState } from 'react';import { HardDrive } from 'lucide-react';import type { StorageInfo } from '@/models';import { storageService } from '@/services/storageService';
const fmt=(n?:number)=>n==null?'No disponible':n>1e9?`${(n/1e9).toFixed(1)} GB`:n>1e6?`${(n/1e6).toFixed(1)} MB`:`${Math.round(n/1e3)} KB`;
export function StorageSummary(){const [info,setInfo]=useState<StorageInfo|null>(null);useEffect(()=>{storageService.inspect().then(setInfo)},[]);return <div className="rounded-lg border border-border bg-card p-4 shadow-sm"><div className="flex items-center gap-2 font-bold"><HardDrive className="size-5 text-primary"/>Almacenamiento local</div><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><Stat label="Datos" value={fmt(info?.data)}/><Stat label="Fotografías" value={fmt(info?.photos)}/><Stat label="Mapas" value={fmt(info?.maps)}/><Stat label="Total usado" value={fmt(info?.total)}/></div><div className="mt-4 rounded-md bg-muted px-3 py-2 text-xs text-muted-foreground">Espacio disponible: <strong className="text-foreground">{fmt(info?.available)}</strong></div></div>};function Stat({label,value}:{label:string;value:string}){return <div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-0.5 font-bold">{value}</p></div>}
